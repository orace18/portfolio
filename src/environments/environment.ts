// Configuration de l'environnement de DÉVELOPPEMENT.
// apiUrl pointe vers l'API NestJS lancée en local (voir backend/README ou backend/.env.example).
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000',
};
