// Configuration de l'environnement de PRODUCTION.
// Remplacez apiUrl par l'URL publique de votre API NestJS déployée (Render, Railway, Fly.io, VM...).
export const environment = {
  production: true,
  apiUrl: 'https://VOTRE-API-EN-PRODUCTION.example.com',
};
